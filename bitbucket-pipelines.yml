clone:
  depth: full

options:
  docker: true

image: threadable/linuxnginxphp_ci_20.04:2024.12.01

definitions:
  services:
    mysql:
      image: mysql:5.7
      environment:
        MYSQL_ROOT_PASSWORD: root
        MYSQL_DATABASE: app
        MYSQL_USER: user
        MYSQL_PASSWORD: password

pipelines:
  branches:
    "devops":
      - step:
          name: Run Tests - PHP 8.3, Laravel 11
          services:
            - mysql
          caches:
            - composer
          script:
            - export LARAVEL_VERSION=11
            - update-alternatives --set php /usr/bin/php8.3
            - curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer
            - composer update --prefer-dist --no-interaction --no-progress --with="illuminate/contracts=^11"
            - vendor/bin/phpunit --display-deprecations --fail-on-deprecation 
